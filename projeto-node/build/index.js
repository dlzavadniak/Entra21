"use strict";
console.log("Teste");
//let numeros: (number | string)[];
//numeros = [true, 0, ""];
let f = (x, y) => {
    return x + y;
};
let numeros;
numeros = f(2.4, 6);
let rect = {
    width: 20,
    height: 30
};
let t = {
    angle: 45,
    type: "isoceles"
};
t = {
    radius: 9
};
